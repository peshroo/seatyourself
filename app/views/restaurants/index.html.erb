<h1>Seat Yourself</h1>

<h2>All Restaurants | <%= link_to "View all users", users_path%></h2>

<% @restaurants.each do |restaurant| %>
  <p><%= link_to restaurant.name, restaurant_path(restaurant.id)%></p>
  <p><%= restaurant.address %></p>
<% end %>
